type Query {
  books: [Book]
  students: [Student!]!
  student(id: ID!): Student
}

type Mutation {
  addStudent(name: String!, age: Int!, class_id: String): Student
  updateStudent(id: ID!, name: String, age: Int, class_id: String): Student
  deleteStudent(id: ID!): Boolean
}

type Book {
  title: String
  author: String
}

type Student {
  id: ID!
  name: String!
  age: Int!
  class_id: String
}
